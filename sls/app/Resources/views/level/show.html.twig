{% extends 'base.html.twig' %}

{% block body %}
    <h1>Level</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ level.id }}</td>
            </tr>
            <tr>
                <th>Levelname</th>
                <td>{{ level.levelname }}</td>
            </tr>
            <tr>
                <th>Creationdate</th>
                <td>{% if level.creationdate %}{{ level.creationdate|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('level_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('level_edit', { 'id': level.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
