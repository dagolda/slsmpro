{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add new document</h1>
<div id= records_entity>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input  type="submit" value="Save" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('employee_pj_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
