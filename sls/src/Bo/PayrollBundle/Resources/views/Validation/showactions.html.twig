	<div class="panel panel-default">
		<div class="panel-body">
			<table>
				<tr>
					<td>
						<a href="{{ path('payroll_validation_list') }}">{{'action.back'|trans([],'BoAdminBundle')}}</a>
					</td>
				{%if app.session.get('rights')['ACCOUNTING']['TS Validation'] is defined and app.session.get('rights')['ACCOUNTING']['TS Validation'].edit==1%}	
					{%if timesheets|length>1%}
						<td>
							<button type="button" class="btn btn-info btn-lg modal-create" data-toggle="modal" data-target="#all_validate">{{'action.validateall'|trans([],'BoAdminBundle')}}</button>
						</td>
					{%endif%}
				{%endif%}
				</tr>
			</table>		
		</div>
	</div>	