	<div class="panel panel-default">
		<div class="panel-body">
			<table>
				<tr>
					<td><a href="{{ path('quiz_show', { 'id': quiz.id }) }}">{{'action.back'|trans([],'BoAdminBundle')}}</a></td>
					<td>
						<button type="button" class="btn btn-info btn-lg modal-delete" data-toggle="modal" data-target="#myModal">
							{{'action.delete'|trans([],'BoAdminBundle')}}
						</button>
					</td>
					<td>
						<a href="{{ path('question_edit', { 'id': question.id,'idquiz': quiz.id }) }}">{{'action.edit'|trans([],'BoAdminBundle')}}</a>
					</td>
					<td>
						<a href="{{ path('question_previous', { 'id': question.id,'idquiz': quiz.id }) }}">{{'action.previous'|trans([],'BoAdminBundle')}}</a>
					</td>
					<td>
						<a href="{{ path('question_next', { 'id': question.id,'idquiz': quiz.id }) }}">{{'action.next'|trans([],'BoAdminBundle')}}</a>
					</td>
					{%if daqb==1%}
					<td>
						<a href="{{ path('question_duplicate', { 'id': question.id,'idquiz': quiz.id }) }}">{{'action.duplicate'|trans([],'BoAdminBundle')}}</a>
					</td>
					<td><button type="button" class="btn btn-info btn-lg modal-create" data-toggle="modal" data-target="#add_question">{{'action.add'|trans([],'BoAdminBundle')}} {{'label.question'|trans([],'BoAdminBundle')}}</button></td>	
					{%endif%}
				</tr>
			</table>		
		</div>
	</div>	
