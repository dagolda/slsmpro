	<div class="panel panel-default">
		<div class="panel-body">
			<table>
				<tr>
					<td class="delete-action">
						{{ form_start(validate_form) }}
							<input type="submit" value="{{'action.confirm'|trans([],'BoAdminBundle')}}">
						{{ form_end(validate_form) }}
					</td>
					<td>
						<a href="{{ path('student_timesheet_index') }}">{{'action.back'|trans([],'BoAdminBundle')}}</a>
					</td>
				</tr>
			</table>		
		</div>
	</div>	