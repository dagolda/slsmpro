	<center>	
		<table>
			<tr>
				<td class="sty-label">Page:</td>
				<td class="page_first">
					<a href="{{ path('employee_page_absences', {'option': 1}) }}" title="{{'label.firstpage'|trans([],'BoAdminBundle')}}">
						<img src="{{ asset('images/actions/first.gif') }}"/>
					</a>
				</td>
				<td>
					<a href="{{ path('employee_page_absences', {'option': 2}) }}" title="{{'label.previouspage'|trans([],'BoAdminBundle')}}">
						<img src="{{ asset('images/actions/prev.gif') }}" />
					</a>
				</td>
				<td><select id="idpage" name="idpage">{%for i in range(1,nb_pages)%}<option value="{{i}}" {%if i==page%}selected="selected"{%endif%}>{{i}}</option>{%endfor%}</select>/{{nb_pages}}
				</td>
				<td>
					<a href="{{ path('employee_page_absences', {'option': 3}) }}" title="{{'label.nextpage'|trans([],'BoAdminBundle')}}">
						<img src="{{ asset('images/actions/next.gif') }}"/>
					</a>
				</td>
				<td>
					<a href="{{ path('employee_page_absences', {'option': nb_pages}) }}" title="{{'label.lastpage'|trans([],'BoAdminBundle')}}">
						<img src="{{ asset('images/actions/last.gif') }}"/>
					</a>
				</td>
			</tr>
		</table>
	</center>