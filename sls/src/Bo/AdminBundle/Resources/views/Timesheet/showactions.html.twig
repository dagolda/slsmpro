	<div class="panel panel-default">
		<div class="panel-body">
			<table>
				<tr>
					{%if timesheet.status==1 or timesheet.status==4%}
					<td>
						<button type="button" class="btn btn-info btn-lg modal-delete" data-toggle="modal" data-target="#myModal">{{'action.delete'|trans([],'BoAdminBundle')}}</button>
					</td>
					{%endif%}
					<td>
						<a href="{{ path('timesheet_index') }}">{{'action.back'|trans([],'BoAdminBundle')}}</a>
					</td>
					{%if timesheet.status==1 or timesheet.status==4%}
					<td>
						<a href="{{ path('timesheet_edit', { 'id': timesheet.id }) }}">Edit</a>
					</td>
					{%endif%}
				</tr>
			</table>		
		</div>
	</div>	